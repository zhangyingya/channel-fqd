package com.tydic.channelview.bizzone;

import javax.annotation.Resource;
import javax.servlet.http.HttpServletRequest;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.ResponseBody;

import com.tydic.channelview.bizzone.bean.BizZone;
import com.tydic.channelview.bizzone.service.BizZoneService;

@Controller
@RequestMapping("/bizzone")
public class BizZoneController {
	
	private final Logger logger = LoggerFactory.getLogger(BizZoneController.class);
	
	/*@Resource
	private BizZoneService bizZoneServiceImpl;
	
	@RequestMapping("queryById")
	@ResponseBody
	public BizZone queryById(HttpServletRequest request) {
		logger.info("start query bizzone info!");
		
		Long bizZoneId = Long.valueOf(request.getHeader("bizZoneId"));
		
		BizZone bizZone = bizZoneServiceImpl.selectByPrimaryKey(bizZoneId);
		
		logger.info("query bizzone info succcess!");
		return bizZone;
	} 
*/
}
