package com.tydic.channelview.operator;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/operator")
public class OperatorController
{
 private final Logger logger = LoggerFactory.getLogger(OperatorController.class);
    
    /*@Resource
    private OperatorService operatorServiceImpl;
    
    @RequestMapping("getOperatorById")
    @ResponseBody
    public OperatorBean selectOperatorById(HttpServletRequest request)
    {
        logger.info("Operator查询方法开始执行");
        Long operatorsId = Long.valueOf(request.getParameter("operatorsId"));
        OperatorBean operatorBean = operatorServiceImpl.findOperatorById(operatorsId);
        logger.info("Operator查询方法执行结束");
        return operatorBean;
        
    }*/
}
